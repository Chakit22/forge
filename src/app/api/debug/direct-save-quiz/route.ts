import { NextResponse } from 'next/server';

export async function GET() {
  return NextResponse.json({ message: 'API route removed - no longer needed' });
}

export async function POST() {
  return NextResponse.json({ message: 'API route removed - no longer needed' });
} 